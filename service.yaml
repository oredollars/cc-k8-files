apiVersion: v1
kind: Service
metadata:
  namespace: firstpractice 
  name: color-checker
  labels:
    app: color-checker
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: color-checker-service